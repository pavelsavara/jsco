import { aliasExportType3 } from '../../hello/wat/hello';
import { expectModelToEqualWat } from './jest-utils';

describe('export', () => {

    test('parse alias', async () => {
        //TODO await expectModelToEqualWat('(alias export 0 "city-info" (type (;3;)))', [aliasExportType3]);
    });
});

