// Nested composition: forwarder composed with forwarder-implementer (which itself composes fwd+impl)
// Tests deeply nested component instantiation with implementer inside
// Used by Scenario K
package jsco:nested-forwarder-implementer;

let inner = new jsco:forwarder-implementer { ... };
let outer = new jsco:forwarder { ...inner, ... };
export outer...;