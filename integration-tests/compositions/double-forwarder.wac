// Two forwarders composed flat — inner satisfies outer's WASI imports
// Used by Scenario G
package jsco:double-forwarder;

let inner = new jsco:forwarder-p2 { ... };
let outer = new jsco:forwarder-p2 { ...inner, ... };
export outer...;
