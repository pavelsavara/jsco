name: test

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read
  packages: read
  pull-requests: write

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        configuration: [Debug, Release]
    env:
      NODE_OPTIONS: --experimental-vm-modules --expose-gc
      Configuration: ${{ matrix.configuration }}

    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v5
      with:
        node-version: 24

    - name: Install npm dependencies
      run: npm ci

    - name: Build (${{ matrix.configuration }})
      run: npm run build

    - name: Run Jest unit tests (${{ matrix.configuration }})
      run: |
        COVERAGE_FLAG=${{ matrix.configuration == 'Debug' && '--coverage' || '--no-coverage' }}
        node --experimental-vm-modules --expose-gc --experimental-wasm-jspi node_modules/.bin/jest $COVERAGE_FLAG --testPathIgnorePatterns integration.test echo-reactor.test hello-world.test use-number-for-int64.test

    - name: Upload coverage (Debug only)
      if: matrix.configuration == 'Debug'
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: coverage/

    - name: Coverage summary (Debug only)
      if: matrix.configuration == 'Debug' && github.event_name == 'pull_request'
      uses: davelosert/vitest-coverage-report-action@v2
      with:
        json-summary-path: coverage/coverage-summary.json
        json-final-path: coverage/coverage-final.json
        file-coverage-mode: all

  release-bundle-size:
    runs-on: ubuntu-latest
    env:
      Configuration: Release
      ContinuousIntegrationBuild: 'true'

    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-node@v5
      with:
        node-version: 24

    - name: Install npm dependencies
      run: npm ci

    - name: Build release bundle
      run: npm run build

    - name: Assert bundle size under limits
      run: |
        check_size() {
          local file=$1 limit=$2 label=$3
          local size
          size=$(stat --format=%s "$file")
          echo "${label}: ${size} bytes (limit: ${limit})"
          if [ "$size" -gt "$limit" ]; then
            echo "::error::${label} size ${size} bytes exceeds limit (${limit} bytes)"
            return 1
          fi
        }
        fail=0
        check_size dist/release/index.js                122880  "index.js (120 KB)"                || fail=1
        check_size dist/release/wasip1-via-wasip3.js     23552  "wasip1-via-wasip3.js (23 KB)"     || fail=1
        check_size dist/release/wasip2-via-wasip3.js     35840  "wasip2-via-wasip3.js (35 KB)"     || fail=1
        check_size dist/release/wasip2-via-wasip3-node.js 45056 "wasip2-via-wasip3-node.js (44 KB)" || fail=1
        check_size dist/release/wasip3.js                 38912  "wasip3.js (38 KB)"                || fail=1
        check_size dist/release/wasip3-node.js            64512  "wasip3-node.js (63 KB)"           || fail=1
        if [ "$fail" -eq 1 ]; then exit 1; fi
        echo "✓ All bundle sizes OK"

    - name: Run demos (Debug build)
      env:
        Configuration: Debug
      run: |
        npm run build
        npm run demo
        npm run demo:verbose

  browser-tests:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/pavelsavara/jsco-browser-tests:playwright-1.59.1-node-24
      credentials:
        username: ${{ github.actor }}
        password: ${{ github.token }}
    env:
      NODE_OPTIONS: --experimental-vm-modules
      HOME: /root

    steps:
    - uses: actions/checkout@v6

    - name: Show browser-test image versions
      run: |
        node --version
        npm --version
        playwright --version

    - name: Install npm dependencies
      run: npm ci

    - name: Build
      run: npm run build

    - name: Run Playwright browser tests
      run: npm run test:browser
